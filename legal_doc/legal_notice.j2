<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Legal Notice</title>
    <style>
        body { font-family: 'Times New Roman', serif; line-height: 1.6; margin: 40px; color: #333; }
        .container { width: 80%; margin: auto; border: 1px solid #ccc; padding: 30px; box-shadow: 2px 2px 8px rgba(0,0,0,0.1); }
        h1 { text-align: center; color: #0056b3; margin-bottom: 30px; font-size: 24px; }
        .address-block { margin-bottom: 20px; }
        .address-block p { margin: 0; }
        .section { margin-bottom: 20px; }
        .section-title { font-weight: bold; margin-bottom: 10px; text-decoration: underline; }
        .content { margin-left: 20px; }
        .signature-block { margin-top: 50px; text-align: right; }
        .signature-line { border-top: 1px solid #000; width: 200px; margin-left: auto; margin-top: 5px; padding-top: 5px; }
        .footer { margin-top: 50px; font-size: 12px; text-align: center; color: #666; }
        .bold { font-weight: bold; }
        .italic { font-style: italic; }
    </style>
</head>
<body>
    <div class="container">
        <h1>LEGAL NOTICE</h1>

        <div class="address-block">
            <p>From:</p>
            <p><span class="bold">{{ sender_name | default('[Sender Name]') }}</span></p>
            <p>{{ sender_address | default('[Sender Address]') }}</p>
            <p>Date: <span class="bold">{{ date_of_notice | default('[Date of Notice]') }}</span></p>
        </div>

        <div class="address-block">
            <p>To:</p>
            <p><span class="bold">{{ recipient_name | default('[Recipient Name]') }}</span></p>
            <p>{{ recipient_address | default('[Recipient Address]') }}</p>
        </div>

        <div class="section">
            <p><span class="bold">Subject:</span> {{ subject | default('[Subject of Notice]') }}</p>
        </div>

        <div class="section">
            <p>Dear Sir/Madam,</p>
            <p>Under instructions from and on behalf of our client, <span class="bold">{{ sender_name | default('[Sender Name]') }}</span>, residing at the above-mentioned address, I hereby serve upon you this legal notice:</p>
        </div>

        <div class="section">
            <div class="section-title">Details of Claim:</div>
            <div class="content">
                <p>{{ details_of_claim | default('[Details of Claim]') | replace('\n', '<br>') | safe }}</p>
            </div>
        </div>

        <div class="section">
            <div class="section-title">Action Demanded:</div>
            <div class="content">
                <p>{{ action_demanded | default('[Action Demanded]') | replace('\n', '<br>') | safe }}</p>
            </div>
        </div>

        <div class="section">
            <div class="section-title">Consequences of Non-Compliance:</div>
            <div class="content">
                <p>{{ consequences | default('[Consequences of Non-Compliance]') | replace('\n', '<br>') | safe }}</p>
            </div>
        </div>

        <div class="section">
            <p>A copy of this notice is retained in my office for further necessary action.</p>
        </div>

        <div class="signature-block">
            <p>Yours faithfully,</p>
            <p>{{ signature | default('[Advocate/Sender Signature]') }}</p>
            <div class="signature-line"></div>
        </div>

        <div class="footer">
            <p>Note: This is a draft legal notice. It is advisable to consult with a legal professional before sending any formal legal communication.</p>
        </div>
    </div>
</body>
</html>
